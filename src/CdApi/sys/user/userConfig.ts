// Administer the behaviour of user account
export default {
    register: {
        notification: {
            email: false,
            socketIo: false,
            webPush: false
        }
    }
}